// slice;
// filter returns array that passes boolean test;
// set; - iterating over a set, use for (value of setName){}
//request.post('http://service.com/upload', {form:{key:'value'}}) - post to url
//request(  use this to get from url
//   "https://jsonplaceholder.typicode.com/posts",
//   { json: true },
//   (err, res, body)

// let numbers = [1, 2, 3, 1, 2, 4, 0, 3, 2];
// let test = new Set();
// let results = [];
// numbers.forEach(number => test.add(number));
// for (number of test) {
//   results.push(number * 2);
// }
// console.log(results);

// // console.log(test.keys(), test.values());

// // console.log(test.size);

// // console.log(test.entries());

// trim() removes whitespace from the string
// toUpperCase
// toLowerCase
// toString
// parseInt(string, base)
// replace(valueToReplace, replaceValue)

// for loop
// for in
// for of (for sets)
// while (condition){code block}
// forEach, end an iteration by returning (if using an if condition)
// do {code block}
// while (condition)

// stack  (last in first ouut)
//     push
//     pop
//     peek

// queue (first in first out)
//     enqueue
//     dequeue
//     size

// linked list
//     made of nodes
//     has head and tail
//     single or doubly linked
//     this.val
//     this.next
//     this.prev (if doubly linked)

// hashmap
//     key value
//     have key, has the value with a hashing function

// BST
//     composed of nodes
//     each parent has two childern
//     left node is always less than parent, right always more than parent
